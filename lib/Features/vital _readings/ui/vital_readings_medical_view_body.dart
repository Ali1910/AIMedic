import 'package:flutter/material.dart';

class VitalReadingsViewBody extends StatelessWidget {
  const VitalReadingsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('قراءات حيوية'),
    );
  }
}
